node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.claude/design-previews/
.claude/worktrees/
.claude/cache/
.claude/skills/docs-crawler-workspace/
.playwright-mcp/
public/og/*.png
.gstack/

# 일회성 디버그·프로브 스크립트. 무시 대상이 아니어서 `git add -A` 에 두 번 딸려 들어갔고
# (PR #307 에서 amend 로 걷어냄) 커밋 후에야 보였다. 이름 규약을 정해 두는 편이
# 매번 조심하는 것보다 낫다.
zz-*
scratch-*
*_scratch_*
probe.tmp.*
